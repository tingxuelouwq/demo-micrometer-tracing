server:
  port: 9090

spring:
  application:
    name: demo-consumer-service

  http:
    serviceclient:
      demo-echo-service:
        base-url: lb://demo-echo-service

logging:
  file:
    name: logs/${spring.application.name}.log
