<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 引入 Spring 环境变量 -->
    <springProperty scope="context" name="appName" source="spring.application.name"/>
    <springProperty scope="context" name="kafkaServers" source="logging.kafka.bootstrap-servers" defaultValue="localhost:9092"/>
    <springProperty scope="context" name="kafkaTopic" source="logging.kafka.topic" defaultValue="application-logs"/>
    <springProperty scope="context" name="profile" source="spring.profiles.active" defaultValue="default"/>

    <!-- ==================== 控制台输出 (开发环境) ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== Kafka Appender (生产环境) ==================== -->
    <!-- 使用 LogstashEncoder 输出 JSON 格式，方便 Logstash 解析 -->
    <appender name="KAFKA" class="com.github.danielwegener.logback.kafka.KafkaAppender">
        <!-- Kafka 配置 -->
        <topic>${kafkaTopic}</topic>
        <producerConfig>bootstrap.servers=${kafkaServers}</producerConfig>
        <producerConfig>acks=1</producerConfig>
        <producerConfig>retries=3</producerConfig>
        <producerConfig>batch.size=16384</producerConfig>
        <producerConfig>buffer.memory=33554432</producerConfig>
        <producerConfig>max.block.ms=5000</producerConfig>

        <!-- 使用 Logstash JSON 编码器 -->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- 包含 MDC 数据（Trace ID 等） -->
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>parentId</includeMdcKeyName>
            <!-- 自定义字段 -->
            <customFields>{"service":"${appName}","profile":"${profile}"}</customFields>
        </encoder>

        <!-- 分区策略：使用 Trace ID 作为 Key，保证同一请求日志有序 -->
        <keyingStrategy class="com.github.danielwegener.logback.kafka.keying.NoKeyKeyingStrategy"/>

        <!-- 异步投递策略 -->
        <deliveryStrategy class="com.github.danielwegener.logback.kafka.delivery.AsynchronousDeliveryStrategy"/>

        <!-- 发送失败时的回退 appender -->
        <appender-ref ref="CONSOLE"/>
    </appender>

    <!-- ==================== 异步包装 Kafka Appender ==================== -->
    <!-- 防止 Kafka 阻塞应用，使用 AsyncAppender -->
    <appender name="ASYNC_KAFKA" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>4096</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="KAFKA"/>
    </appender>

    <!-- ==================== 日志级别配置 ==================== -->
    <!-- 根日志：同时输出到控制台和 Kafka -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_KAFKA"/>
    </root>

    <!-- 避免 Kafka 客户端自身的日志递归发送 -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="com.github.danielwegener.logback.kafka" level="WARN"/>
</configuration>